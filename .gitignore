# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Data pipeline outputs (regenerated by /pipeline, not committed as source)
data/raw/
data/interim/
*.parquet
# ...except published site data: real deployed assets the AskAI feature's
# client-side DuckDB-Wasm instance queries, not a pipeline intermediate.
!site/assets/data/*.parquet

# Jekyll
site/_site/
site/.jekyll-cache/
site/.jekyll-metadata
site/.bundle/
site/vendor/

# Node
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
*.swp
